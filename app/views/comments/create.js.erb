<% if @comments %>
    $("#comments").html('<%= escape_javascript render 'comments/list' %>');
    $("#comment_body").val('');
    jQuery(function($) {
        observe_element('ajax_paginator');
    });
<% else %>
    system_message('<%= escape_javascript system_messages %>');
<% end %>